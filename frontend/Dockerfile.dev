# Development Dockerfile with Hot-Reloading Support
FROM node:20-alpine

WORKDIR /app

# 의존성 설치를 위한 package.json 복사
COPY package*.json ./

# 의존성 설치
RUN npm install

# 전체 소스 코드는 볼륨 마운트로 제공됨
# COPY는 하지 않음 - docker-compose에서 volume으로 연결

# Next.js 개발 서버 포트
EXPOSE 3000

# 개발 서버 실행 (핫 리로딩 활성화)
CMD ["npm", "run", "dev"]
