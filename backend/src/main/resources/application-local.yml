spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bananadate
    username: ${DB_USERNAME:bananadate}
    password: ${DB_PASSWORD:bananadate123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: false

# External APIs
claude:
  api:
    key: ${CLAUDE_API_KEY:dummy-key-for-testing}

kakao:
  api:
    rest-key: ${KAKAO_REST_API_KEY:12aeb9648f81c9812fe3e738957ac43a}

logging:
  level:
    com.bananadate: DEBUG
